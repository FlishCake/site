import React from "react";

import Layout from "../components/layout";

const title = "Главная";

const content => {
    <h1>Test</h1>
};

export default () => (
  <Layout headerText={title} content={content} />
);
